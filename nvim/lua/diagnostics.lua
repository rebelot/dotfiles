vim.diagnostic.config({
    float = {source = 'always'},
    virtual_text = false, -- , source = 'always'},
    underline = true,
    signs = true,
    update_in_insert = false,
    severity_sort = true,
})
